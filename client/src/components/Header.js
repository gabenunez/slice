import React from 'react';

function Header() {
    return (
        <header>
            <h1>Slice O' Pizza</h1>
            <h2>Because finding the best pizza deal shouldn't be so hard.</h2>
        </header>
    );
}

export default Header;